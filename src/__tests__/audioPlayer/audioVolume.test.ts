import { render, screen } from "@testing-library/react";

describe("AudioPlayer Volume", () => {
    test("render volume when isVolumeBarOnScreen is true", () => {
    render(<button>Test</button>);
    })
} )