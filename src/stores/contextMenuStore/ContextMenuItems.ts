import type { ContextMenuItem } from "../../types";

const menuItems: ContextMenuItem[] = [
  { label: "Play next", action: () => {} },
  { label: "Add to playlist", action: () => {} },
  { label: "Delete from playlist", action: () => {} },
  { label: "Clear playlist", action: () => {} },
];

export { menuItems };
